import { CollectionView } from './CollectionView.jsx';
export default function Activities() { return <CollectionView resource="activities" title="Activity log" description="Every run, walk, and strength session counts." renderItem={(activity) => <article className="resource-card" key={activity._id ?? `${activity.type}-${activity.completedAt}`}><span className="card-kicker">{activity.type}</span><h2>{activity.points} points</h2><p>{activity.durationMinutes} minutes</p><p className="muted">{new Date(activity.completedAt).toLocaleDateString()}</p></article>} />; }